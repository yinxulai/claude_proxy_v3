{
  "name": "claude-proxy-v3",
  "version": "3.0.0",
  "description": "Claude API Proxy v3 - Complete Claude API surface support",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/server.js",
    "server": "tsx src/server.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "tiktoken": "^1.0.15",
    "typescript": "^5.9.3",
    "wrangler": "^4.60.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.1.0"
  }
}
